# Frontend Environment Variables - Copy this file to .env and add your actual values

# Backend API URL (use your deployed backend URL in production)
REACT_APP_API_BASE_URL=http://localhost:5000/api

# Legacy API Keys (kept for fallback - these are now handled by the backend)
# These are optional since the backend now handles API calls
REACT_APP_GEMINI_API_KEY=your_gemini_api_key_here
REACT_APP_ELEVENLABS_API_KEY=your_elevenlabs_api_key_here